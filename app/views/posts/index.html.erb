<section class="section">
  <div class="container">
    <div class="columns is-centered">
      <%= render 'top_posts' %>
      <%= render 'feed' %>
      <div class="column is-one-quarter">
        <%= render 'users/friend_requests' if @friend_requests.any? %>
        <%= render 'users/suggested_friends' %>
      </div>
    </div>
  </div>
</section>